on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: newsdictstandard.azurecr.io
        username: ${{ secrets.acr_newsdictstandard_username }}
        password: ${{ secrets.acr_newsdictstandard_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t newsdictstandard.azurecr.io/newsdictprod:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push newsdictstandard.azurecr.io/newsdictprod:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_newsdictprod_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep default | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace default`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: default
        container-registry-url: newsdictstandard.azurecr.io
        container-registry-username: ${{ secrets.acr_newsdictstandard_username }}
        container-registry-password: ${{ secrets.acr_newsdictstandard_password }}
        secret-name: newsdictproddockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: default
        manifests: |
          manifests/deployment-1.yml
          manifests/service-1.yml
        images: |
          newsdictstandard.azurecr.io/newsdictprod:${{ github.sha }}
        imagepullsecrets: |
          newsdictproddockerauth
